version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: ncr-postgres
    environment:
      POSTGRES_DB: ncr
      POSTGRES_USER: ncr
      POSTGRES_PASSWORD: ncr
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ncr"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: .
    container_name: ncr-backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_HOST: ncr-postgres
      POSTGRES_DB: ncr
      POSTGRES_USER: ncr
      POSTGRES_PASSWORD: ncr
    ports:
      - "4000:4000"
    restart: unless-stopped

volumes:
  pgdata:

